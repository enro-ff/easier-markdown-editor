# 项目未来的展望，不用执行，主要是考虑后续进行的空间
- 优化图片显示
    0. 了解sha-256图片哈希，真人干。
    1. 存储图片高度
    2. 读取图片高度在imageWidget
    3. 添加canvas压缩（只在编辑器显示区域）
    4. 添加存储map策略，使image压缩后编辑器显示区域最多存储10个，预览区高清图片只能有一个。
    5. 加入内存监听，占用过高就手动释放（）
- 优化打印
    webllm 换成qwen 3.5 0.8b本地（暂时不做）
    webllm 在加载过程中优先请求后端模型服务（暂时不做）
    webllm 放入webworker通讯
    优化iframe
        1.预览区域动态测算比例，使用rem
        2.计算高度手动设置分页，要用存储的图片高度
        3.支持A3之类的window.print的其他格式
- 设置背景
    1. 支持静态背景， 背景存放到cache api中
    2. 加入蒙版层，可动态调整透明度
    3. 支持mp4视频背景
    4. 支持多背景切换。
    5. 加入ffmpeg，压缩视频（像素，帧率）（或尝试webassembly，待定）
    6. 加入背景轮换
- 性能优化
    懒加载ffmpeg的包
    把视频压缩，webllm都加入内存监控，动态防卫内存

